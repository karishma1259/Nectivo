spring.application.name=nectivo-backend

# ---- Database (values come from environment variables in production) ----
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/nectivo_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

server.port=${PORT:8080}

# ---- JWT ----
jwt.secret=${JWT_SECRET:nectivo-super-secret-key-change-this-in-production-1234567890}
jwt.expiration-ms=${JWT_EXPIRATION:86400000}

# ---- CORS ----
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173}
